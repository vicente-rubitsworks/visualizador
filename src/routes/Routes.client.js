import { ClientLayout } from "../Layouts";
import { Home } from "../Pages/Home";

const routesClient = [
  {
    path: "/",
    layou: ClientLayout,
    component: Home,
  },
];

export default routesClient;
